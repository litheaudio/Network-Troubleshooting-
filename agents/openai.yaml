interface:
  display_name: "Lithe Speaker Network Check"
  short_description: "Friendly live speaker and network support"
  default_prompt: "Use $diagnose-lithe-speaker-network for a real case: greet me, ask for my speaker IP, ask exactly five key questions, then run the checks."
