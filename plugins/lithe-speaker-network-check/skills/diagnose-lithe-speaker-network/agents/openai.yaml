interface:
  display_name: "Lithe Speaker Network Check"
  short_description: "Find evidence and safely fix speaker dropouts"
  default_prompt: "Use $diagnose-lithe-speaker-network for my real speaker issue. Ask one question per turn, run locale-safe target-only checks, analyse the approved local speaker log without retaining the raw file, correlate time-weighted evidence, explain the targeted product-aware fix and why, then action only the exact reversible change I separately approve and verify the result."
